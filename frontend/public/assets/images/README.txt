Drop the RNHS school seal here as `rnhs-logo.png` (or .jpg/.svg).

The app references this image at:
   /assets/images/rnhs-logo.png

If the file is missing, the UI hides the logo gracefully (onerror handler)
so the rest of the page still renders. Recommended dimensions: 256x256+
square, transparent background.
