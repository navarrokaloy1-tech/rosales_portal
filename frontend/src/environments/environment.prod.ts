// Production environment used by `ng build` (swapped in via angular.json fileReplacements).
// Replace the URL below with your deployed backend's URL on Render.
export const environment = {
  production: true,
  apiUrl: 'https://rosales-portal-api.onrender.com/api',
};
